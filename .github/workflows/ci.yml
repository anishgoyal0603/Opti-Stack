name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run test suite
        run: pytest -q

  unit-tests-no-sdk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install test deps only (deliberately no google-genai)
        run: pip install pytest pytest-cov python-dotenv
      - name: Run dependency-free unit tests
        # --no-cov: pyproject.toml applies a 70% coverage floor to every
        # pytest run, which a deliberate single-file subset can never meet.
        # This job proves imports work without the Gemini SDK, not coverage.
        run: pytest tests/test_security_scanner.py -q --no-cov